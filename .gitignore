# R session files
.Rhistory
.Rdata
.RData
.Ruserdata

# RStudio project files
.Rproj.user/
*.Rproj

# R package build artifacts
src/*.o
src/*.so
src/*.dll
*.tar.gz

# Check/build directories
*.Rcheck/

# OS files
.DS_Store
Thumbs.db

# Byte-compiled / cached
inst/doc

# Credentials / secrets
.Renviron
.Rprofile

# Internal review notes (not for distribution)
CODE_REVIEW.md

# Internal planning notes (not for distribution)
dev/BACKLOG*.md

# Scratch output from assistant sessions (not for distribution)
Claude outputs/

# pkgdown output. The site is built by .github/workflows/pkgdown.yaml and
# served from the gh-pages branch; a local pkgdown::build_site() or
# dev/render_vignette.R writes here and nothing in docs/ is committed.
docs/
