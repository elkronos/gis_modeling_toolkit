% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-surface.R
\name{predict_surface}
\alias{predict_surface}
\title{Predict a fitted spatial model onto a regular grid}
\usage{
predict_surface(
  object,
  grid = NULL,
  cell_size = NULL,
  n_cells = 10000L,
  boundary = NULL,
  covariates = NULL,
  chunk_size = 5000L,
  se = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{spatial_fit} (e.g. from \code{fit_gwr_model()} or
\code{fit_bayesian_spatial_model()}).}

\item{grid}{Optional \code{sf} POINT layer to predict onto.  When
\code{NULL}, a regular grid is built over the training extent.}

\item{cell_size}{Grid resolution in CRS units.  Ignored when \code{grid} is
supplied; when \code{NULL}, derived from \code{n_cells}.}

\item{n_cells}{Approximate cell count used to derive \code{cell_size}.
Default 10000.}

\item{boundary}{Optional polygonal \code{sf}/\code{sfc}; grid points outside
it are dropped.}

\item{covariates}{Optional \code{sf} layer carrying the model's predictors.
Required when the model has predictors and \code{grid} does not already
contain them.  Values are taken from the nearest feature.}

\item{chunk_size}{Rows per prediction call. Default 5000.}

\item{se}{Logical; also return a standard-error/posterior-SD column where the
backend supports it.  Default FALSE.}

\item{...}{Passed to \code{predict()}.}
}
\value{
An \code{sf} POINT layer with a \code{.pred} column (and
\code{.pred_se} when \code{se = TRUE} and available).  The grid resolution
is attached as attribute \code{"cell_size"}.
}
\description{
Builds a prediction surface over the extent of the training data (or over a
grid you supply), predicts in chunks, and returns an \code{sf} layer.
}
\details{
\code{predict()} on a \code{spatial_fit} requires \code{newdata} to be
constructed by hand, which makes the most common downstream task -- produce
a map -- more work than it should be.  This wraps the grid construction,
covariate join, chunking and CRS handling.

Prediction over a grid is embarrassingly parallel in the sense that rows do
not interact, so it is chunked: for \code{bayesian_fit} the posterior draw
matrix is \code{n_draws x n_newdata}, which will exhaust memory on a fine
grid long before the fit itself would.
}
\examples{
\dontrun{
  fit  <- fit_gwr_model(pts, "y", "x")
  surf <- predict_surface(fit, n_cells = 2000)
  plot(surf[".pred"])
}
}
\concept{prediction}
